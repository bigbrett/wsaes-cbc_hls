// ==============================================================
// File generated by Vivado(TM) HLS - High-Level Synthesis from C, C++ and SystemC
// Version: 2016.4
// Copyright (C) 1986-2017 Xilinx, Inc. All Rights Reserved.
// 
// ==============================================================

#ifndef __linux__

#include "xstatus.h"
#include "xparameters.h"
#include "xaescbc.h"

extern XAescbc_Config XAescbc_ConfigTable[];

XAescbc_Config *XAescbc_LookupConfig(u16 DeviceId) {
	XAescbc_Config *ConfigPtr = NULL;

	int Index;

	for (Index = 0; Index < XPAR_XAESCBC_NUM_INSTANCES; Index++) {
		if (XAescbc_ConfigTable[Index].DeviceId == DeviceId) {
			ConfigPtr = &XAescbc_ConfigTable[Index];
			break;
		}
	}

	return ConfigPtr;
}

int XAescbc_Initialize(XAescbc *InstancePtr, u16 DeviceId) {
	XAescbc_Config *ConfigPtr;

	Xil_AssertNonvoid(InstancePtr != NULL);

	ConfigPtr = XAescbc_LookupConfig(DeviceId);
	if (ConfigPtr == NULL) {
		InstancePtr->IsReady = 0;
		return (XST_DEVICE_NOT_FOUND);
	}

	return XAescbc_CfgInitialize(InstancePtr, ConfigPtr);
}

#endif

